# Handoff · 2026-07-01 · Relanzamiento fase 1–3: internas niveladas, home intacto

Sesión de planificación + ejecución del **relanzamiento de la web completa**. Se decidió con Rafa el camino (la landing SE CONVIERTE en el home; nada de resucitar el home v1) y se ejecutaron las fases 1–3 **sin tocar la landing**, que es donde cae el tráfico pago. Commit `aa5179d`, deployado por push.

---

## 1 · TL;DR

- **Auditoría completa (fase 1)**: las internas estaban ya alineadas con la landing (cero regresiones de copy) y con la app en vivo (precios, descuentos, cancelaciones, pagos, horarios, capacidades y m² verificados 1 a 1 contra Supabase). Lo que faltaba: fotos en fichas, los 2 espacios inactivos, y el form en las internas.
- **Fotos reales en todo el catálogo**: 32 webp convertidas de las fotos de la app (`proyectos/espacio1010/public/fotos` → `public/fotos/espacios/`, sharp con `.rotate()` por EXIF). Ficha con carrusel (`FotoCarrusel.astro`), cards con portada (`EspacioCard.astro` renovada — la usan /los-espacios, /para/[slug], /alquiler-consultorio-montevideo y "otros espacios"). Fuera los 4 carteles "Fotos reales, muy pronto".
- **/los-espacios muestra los 10 espacios**: nueva sección **"Próximamente"** con Espacio 04 (PB, 17 m²) y Espacio 15 (P1, 11 m²) como tarjeta simple sin ficha ni CTA (decisión Rafa). `ESPACIOS_PROXIMOS` en `site.ts`; al terminarlos pasan al array `ESPACIOS`.
- **Form de registro pre-footer en TODAS las internas** (`RegistroForm.astro`, renderizado por `BaseLayout` arriba del `Footer`): copia extraída del form de la landing, con `origen` = pathname de la página. Todos los CTAs internos pasaron de `/#registro` a `#registro` (ya no te sacan de la página). **Verificado end-to-end en dev contra Supabase de producción**: insert con `origen: '/precios'` + `dataLayer lead_preregistro` (1 solo push) + redirect con cupón y campos precargados. Lead de prueba borrado.
- **Bug real de view transitions arreglado**: los scripts de `Nav` (menú mobile), `Enhancements` (`data-reveal` → **contenido quedaba invisible** tras navegar entre internas) y `FloatingWhatsApp` no se re-ejecutaban con ClientRouter. Ahora todo se re-engancha en `astro:page-load` con guards anti doble-binding. Lenis quedó singleton a nivel módulo.
- **La landing (`index.astro`) y `LandingLayout` NO se tocaron.** `/privacidad` sigue en LandingLayout a propósito (la linkea el form de la landing).

## 2 · Decisiones tomadas con Rafa

1. **Arquitectura del relanzamiento**: la landing evoluciona a home completo (opción A); el home v1 borrado el 06-02 NO se resucita. **La pauta seguirá apuntando a `/`** — no se crea `/empeza`: el Nav lleva su propio CTA al form, las internas devuelven al embudo, y se evita mantener dos copias del copy. Guardrail: mirar conversión del tráfico pago 2–3 semanas post-switch (Clarity + `lead_preregistro`); si cae, recién ahí se clona una landing de pauta.
2. **Los 10 espacios en la web** (incluidos los inactivos de la app) → 04 y 15 como "Próximamente", tarjeta simple.
3. **Form en cada interna** → como banda pre-footer (no dentro del footer oscuro).
4. **Ejecutar todo sin tocar el home** hasta verificar — idea de Rafa, correcta.

## 3 · Estado de la app (verificado en vivo 2026-07-01)

- `consultorios`: 10 filas — activos 01/02/03/11/12/13/14; **inactivos 04, 15 y Sala Arcos** (la web ya la maneja como "se coordina por WhatsApp", coherente).
- Cupón `BIENVENIDA1010`: activo, 100%, 1 reserva/usuario, vence 31/12/2026 — **48 usos**.
- Config: descuentos 20h→10% / 40h→20% · cancelación 24 h / 50% / 1 h · tope fijas 20% · vencimiento 10 días · app reserva 07–24 · 150 días — **todo coincide con lo publicado**.

## 4 · Lo que queda (fases 4–5, próxima sesión)

1. **Migrar el home a BaseLayout** (Nav + Footer + form compartido `RegistroForm`, borrar el form inline de `index.astro`): el paso chico final. Recordar: `/privacidad` migra junto; revisar la barra CTA sticky mobile (hoy ancla a `#registro` de la propia landing, seguiría OK).
2. Interlinking desde el home hacia las internas (cards de "Los espacios" → fichas, galería → /el-lugar, etc.).
3. Avisar a la agencia del cambio (cortesía; las URLs de campaña no cambian).
4. SEO: revisar metas/OG tras el switch; `priceValidUntil: 2026-12-31` en schema Product tiene TODO para antes de 2027.
5. Fotos de 04 y 15 + descripciones reales cuando Rafa los termine → pasarlos de `ESPACIOS_PROXIMOS` a `ESPACIOS`.

## 5 · Notas técnicas / de proceso

- **Patrón obligatorio para scripts en páginas con ClientRouter**: bindear en `astro:page-load` + llamada directa + guard `dataset` (ver `FotoCarrusel`, `RegistroForm`, `Nav`, `Enhancements`). Un script "clásico" solo corre en la primera carga y muere en la primera view transition.
- **NO tocar** identificadores `preregistro-form`, `preregistro-exito` ni el evento `lead_preregistro` (GTM/agencia). El push al dataLayer va SIN payload (PII prohibida — regla del 06-11).
- Las fotos de la app se toman del repo hermano (`C:\Users\rafac\proyectos\espacio1010\public\fotos`), no del Drive. `sharp` con `.rotate()` antes de `.resize()` (EXIF).
- El preview MCP quedó con viewport 0×0 esta sesión (screenshot se cuelga, clicks de coordenadas no navegan) — se verificó todo por DOM con `preview_eval` + fetch; el form sí se probó end-to-end.
- `Footer.astro` perdió el `mt-32` (el form pre-footer hace de separador).
